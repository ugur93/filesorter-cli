# Filesorter

Sort files by year

Initializing
```bash
python -m venv (pwd)
source <location>

```

Run sorter

````bash
python filesorter_cli.py sortyear "Y:\location\to\source" "Y:\location\where\to\move"

``````